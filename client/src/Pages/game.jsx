import { gameToDisplayed } from "../App";

const game = (gameToDisplayed) => {
  return (
    <div>
      <h2>gameToDisplayed.name</h2>
    </div>
  );
};

export default game;

//recupérer le jeu stocké dans le state et l'afficher
